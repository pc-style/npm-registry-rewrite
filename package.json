{
  "name": "registry-trust",
  "version": "0.1.0",
  "type": "module",
  "private": true,
  "bin": {
    "registry-trust": "./src/cli.ts"
  },
  "scripts": {
    "check": "bun test && bun run typecheck",
    "test": "bun test",
    "demo": "bun run src/cli.ts demo is-odd",
    "review": "bun run src/cli.ts review",
    "typecheck": "bunx tsc --noEmit",
    "demo:full": "bash scripts/demo.sh"
  },
  "dependencies": {
    "tar-stream": "^3.1.7"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/tar-stream": "^3.1.4",
    "typescript": "^6.0.3"
  }
}
